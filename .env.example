# Copy to .env in the same folder as export_erpwe_comments.py — the script loads it automatically.
# Jira Server / Data Center (Basic Auth)
JIRA_BASE_URL=http://jira.domain.com:8080/jira
JIRA_USER=your.login
JIRA_PASSWORD=your_password_or_api_token
